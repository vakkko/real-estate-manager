import styled from "styled-components";

export const ButtonsDiv = styled.div`
  & > h3 {
    font-size: 1.6rem;
    line-height: 100%;
  }

  & > div {
    display: flex;
    gap: 8.4rem;
    margin-top: 0.8rem;
  }
`;
