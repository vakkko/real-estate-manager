import styled from "styled-components";

export const SelectDiv = styled.div`
  & > label {
    font-weight: 600;
    font-size: 1.4rem;
    line-height: 100%;
  }

  & > select {
    margin-top: 0.5rem;
  }
`;
