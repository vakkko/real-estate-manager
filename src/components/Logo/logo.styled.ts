import styled from "styled-components";

export const Header = styled.header`
  border-bottom: 1px solid var(--lightGray);
  margin-bottom: 7.7rem;
  & img {
    margin: 3.8rem 0 3.8rem 16.2rem;
  }
`;
